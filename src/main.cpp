#include <bits/stdc++.h>
#include "gao.hpp"

using namespace std;

int main(void) {
    uint64_t key = 1145141919810810810;
    wstring path;
    wcin >> path;
    huffman_compessor c;
    xorplus_cryptor x(key);
    //auto a = gao::gao(p1, c, x);
    //gao::ungao(p2, a, c, x);

    return 0;
}